"""Case utilities for the wave-equation system.

Submodules are imported explicitly (e.g. ``from .sample import ...``); this file
exists so the directory is a regular package rather than a PEP 420 namespace
package, which keeps ``setuptools.packages.find`` behaviour version-independent.
"""
